# libharu C# Port

This tree is a managed source migration of libharu for .NET 9. It is not a P/Invoke binding and does not load `libhpdf.dll`.

## Build

```powershell
dotnet build LibHaruSharp.sln
dotnet run --project csharp\LibHaru.SmokeTests\LibHaru.SmokeTests.csproj
```

The smoke test writes `artifacts/libharu-managed-smoke.pdf`.
Compatibility demo ports write generated PDFs and `.structure.txt` regression profiles under `artifacts/compatibility-demos`.
When Poppler `pdftoppm` is available, selected first-page renders are checked against `csharp/LibHaru.SmokeTests/fixtures/visual-reference.tsv` and write pixel profiles under `artifacts/rendered`.
Exact upstream/reference PDF comparisons are manifest-driven through `csharp/LibHaru.SmokeTests/fixtures/reference-output.tsv`; rows become byte-for-byte or SHA-256 checks as soon as stable upstream fixtures are added.

## Current Managed Slice

- `hpdf_objects.c`, `hpdf_array.c`, `hpdf_dict.c`, `hpdf_name.c`, `hpdf_number.c`, `hpdf_real.c`, `hpdf_string.c`: represented by `Internal/PdfObjects.cs`, including direct/proxy ownership semantics, exact typed array/dictionary base/subclass lookup validation, managed stream-vs-dictionary type guards, direct raw objects, and encrypted string/binary writing.
- `hpdf_streams.c`, `hpdf_xref.c`: represented by `Internal/PdfWriter.cs` and the writer in `Public/PdfDocument.cs`.
- `hpdf_doc.c`: `New`/`NewEx`, current document manager helpers (`NewDoc`, `FreeDoc`, `HasDoc`, `FreeDocAll`, document/page memory-manager accessors), page tree configuration, `AddPage`, `InsertPage`, `GetPageByIndex`, `GetFont`, info attribute getters/setters including date attributes, stream/file saving, stream chunk reads, and layout/mode/viewer metadata are implemented.
- `hpdf_pages.c`, `hpdf_page_operator.c`: page sizing, boundary/rotate/zoom metadata, text operators including text-rectangle helpers, path painting including even-odd aliases, clipping aliases, color, line style, graphics-state save/restore, ellipse/arc/circle helpers, shared content-stream arrays, and page graphics-state getters are implemented.
- `hpdf_fontdef_base14.c`, `hpdf_font.c`: built-in Base14 Type1 font objects now flow through the shared managed font-program and single-byte encoder path with generated upstream width tables/descriptors and FontSpecific defaults for Symbol/ZapfDingbats.
- `hpdf_fontdef_type1.c`, `hpdf_font_type1.c`: Type1 AFM parsing, PFB/PFA font program loading, font descriptors, widths arrays, and `/FontFile` embedding are started.
- `hpdf_fontdef_tt.c`, `hpdf_font_tt.c`: TrueType table parsing is started for `head`, `hhea`, `maxp`, `hmtx`, `cmap` formats 0/4/6/10/12/13, `name`, `OS/2`, optional `post`, TTC face selection, embedding-permission checks, lower-level TT font-def handles, simple-font widths, descriptors, and `/FontFile2` embedding. Embedded TrueType font programs now emit deterministic dense glyph subsets from actual simple/composite text use, including composite-glyph component closure/remapping, rebuilt subset `cmap`/`hmtx`/`loca` tables, explicit CIDToGIDMap streams, and save-time ToUnicode CMaps for Identity and predefined CMap Type0 paths.
- `hpdf_encoder.c`, `hpdf_encoder_utf.c`, `hpdf_encoder_jp.c`, `hpdf_encoder_kr.c`, `hpdf_encoder_cns.c`, `hpdf_encoder_cnt.c`: single-byte encoder infrastructure is started for Standard, WinAnsi/CP1252, FontSpecific, ISO8859 aliases, CP125x aliases, MacRoman, and KOI8-R names, with generated upstream Unicode maps plus public encoder handles and introspection for type, byte type, Unicode lookup, and writing mode. UTF/Identity encoding now has a managed Type0/CID TrueType path with generated ToUnicode CMaps; Japanese/Korean/Simplified Chinese/Traditional Chinese CMap entry point names are registered for predefined CID flows. The upstream CJK Unicode maps, CMap CID ranges, exact lead/trail byte classifiers, and JP EUC-H/EUC-V entries are generated into the managed port for predefined CMap introspection.
- `hpdf_encrypt.c`, `hpdf_encryptdict.c`: Standard Security revision 2/3 password handling, permissions, encryption dictionary preparation, file IDs, encryption dictionary validation, user/owner password authentication, and per-object RC4 encryption/decryption for strings, binary values, and streams are implemented.
- `hpdf_error.c`, `hpdf_error.h`: full status-code table, document error state, detail codes, reset/check behavior, and callback dispatch are implemented.
- `hpdf_streams.c`, stream filter portions of `hpdf_dict.c`: stream filter flags now write `/Filter` arrays and array-shaped `/DecodeParms`, with Flate encoding for page content, raw image streams, and metadata streams; JPEG streams use `DCTDecode`.
- `hpdf_image.c`, `hpdf_image_png.c`, `hpdf_image_ccitt.c`: JPEG memory/file streams, PNG memory/file loading, raw memory/file images, 1-bit raw images with CCITT Group 4 encoding, image masks, soft masks, color-key masks, Indexed PNG color spaces, PNG CRC/order validation, PNG gAMA/cHRM/sRGB/iCCP color metadata mapping, image validators, page image XObject resources, and form XObject construction helpers from images/white rectangles are started.
- `hpdf_catalog.c`, `hpdf_destination.c`, `hpdf_outline.c`, `hpdf_page_label.c`: destinations, open actions, outlines, page labels, viewer preferences, and catalog names dictionaries are started.
- `hpdf_annotation.c`: link, URI link, text, free text, square/circle, text-markup, popup, stamp, projection, widget, and basic 3D annotations are started with border, highlight, icon, color, opened, contents, title, JavaScript action, generic text-markup, widget appearance, and projection ExData setters. Specialized widget white-while-print appearances now mirror the C helper's AP/MK/FT/F/T dictionary shape, and generic AP streams preserve named normal/rollover/down appearance states with appearance-local Font/XObject resources.
- `hpdf_namedict.c`, `hpdf_pdfa.c`: embedded file streams/filespecs, embedded-file name trees, catalog associated files, PDF/A conformance metadata, output intent dictionaries with ICC profile streams, ICC memory/file loading aliases, save-time PDF/A output-intent/XMP/conformance guardrails, PDF/A embedded-file relationship defaults/restrictions, and large multi-leaf name-tree fixtures are started.
- `hpdf_u3d.c`, `hpdf_ext_gstate.c`, `hpdf_shading.c`: U3D streams, 3D views with perspective/orthographic projection, matrix cameras, cross-section toggles, nodes, C3D/PD3 measures, extended graphics states, and axial/radial/free-form triangle mesh shadings are started.
- `demo/arc_demo.c`, `demo/attach.c`, `demo/character_map.c`, `demo/chfont_demo.c`, `demo/encoding_list.c`, `demo/encryption.c`, `demo/ext_gstate_demo.c`, `demo/font_demo.c`, `demo/grid_sheet.c`, `demo/image_demo.c`, `demo/jpeg_demo.c`, `demo/jpfont_demo.c`, `demo/line_demo.c`, `demo/link_annotation.c`, `demo/outline_demo.c`, `demo/outline_demo_jp.c`, `demo/pdf_a_conformance.c`, `demo/permission.c`, `demo/png_demo.c`, `demo/raw_image_demo.c`, `demo/slide_show_demo.c`, `demo/text_annotation.c`, `demo/text_demo.c`, `demo/text_demo2.c`, `demo/ttfont_demo.c`, and `demo/ttfont_demo_jp.c`: managed compatibility demo ports now run from the smoke app and generate structural PDF profiles for regression coverage. `demo/make_rawimage.c` is tracked in the generated inventory as a utility fixture generator rather than a PDF demo.
- `bindings/c#`: intentionally not used as an implementation source. The managed `HPdf` facade mirrors common `HPDF_*` entry points for migration convenience.

## Remaining Migration Work

- Current validation snapshot: `dotnet build LibHaruSharp.sln` succeeds with no warnings/errors, and `dotnet run --project csharp\LibHaru.SmokeTests\LibHaru.SmokeTests.csproj` generates the managed smoke PDFs plus 28 compatibility-demo PDFs with matching structural profiles.
- Recently closed `include/hpdf.h` gaps: encoder introspection, page graphics-state getters, current document manager helpers, page memory-manager/document accessors, page tree configuration, page boundary/rotate/zoom helpers, remaining page graphics/text operator aliases, ICC memory/file loading aliases, output-intent aliases over loaded ICC profiles, image/white-rect form XObject construction helpers, CJK/UTF font-encoding entry points, document layout/view/info getters, date info setters, stream chunk reads, font text measurement, TT font-def handles, shared content-stream helpers, generic text-markup/widget aliases, and projection ExData helpers are now covered by smoke assertions.
- Error model parity: full status table and core set/check/raise/reset behavior are implemented for the migrated slice; current document, page, font, writer, save, compression, layout/mode, encryption, encoding lookup, PDF-name validation, destination, annotation, image, graphics state, ICC profile, form XObject, and selected 3D validation paths record document-owned errors before throwing. Open task: continue applying this pattern as new C modules and object subclasses are migrated.
- Collections and object semantics: base object classes, array/dictionary ownership edge cases, indirect proxy references, direct raw objects, encryption-aware string/binary writing, exact typed lookup validation, dictionary/array clearing for document reset, and module-specific subclass flags/validators for the migrated catalog, pages/page, font, outline, destination, annotation, encryption, ExtGState, name dictionary/tree, image/form XObject, ICC profile, and shading modules are implemented. Open task: add validators only as additional C modules and object subclasses require them.
- Compression: page content, raw images, PNG image data, soft masks, metadata, embedded files, JavaScript, ICC streams, and generated form XObjects support Flate where appropriate; JPEG streams use DCT filters; 1-bit raw images can emit CCITT Group 4 streams with DecodeParms; stream filter dictionary handling emits libharu-style filter arrays for Flate, DCT, CCITT, ASCIIHex, ASCII85, and DecodeParms. No known compression parity task is currently called out beyond future module coverage.
- Fonts and encoders: Type1 AFM/PFB, generated Base14 width tables/descriptors, simple TrueType parsing/embedding, save-time dense TrueType `/FontFile2` subsets from actual encoded text glyph use with composite-glyph closure/remapping, explicit CIDToGIDMap streams, broader Identity/predefined-CMap ToUnicode output, TrueType cmap formats 0/4/6/10/12/13, a TrueType Type0/CID UTF path, predefined Japanese/Korean/Simplified Chinese/Traditional Chinese CID font names, public encoder handles/introspection, UTF-8 registration, generated upstream single-byte encoder maps, generated upstream CJK Unicode/CID CMap data, exact CJK byte classifiers, JP EUC-H/EUC-V entries, and generated built-in CID metric tables/descriptors are covered by smoke/compatibility output. Open tasks: additional OpenType/CFF coverage, deeper vertical TrueType metrics, and wider real-font compatibility fixtures.
- Images and XObjects: JPEG, PNG, raw memory/file images, 1-bit raw CCITT Group 4 images, image masks, soft masks, color-key masks, DeviceGray/RGB/CMYK raw color spaces, Indexed PNG color spaces, PNG CRC/order validation, PNG gAMA/cHRM/sRGB/iCCP color metadata mapping, image validation, page image/form XObject resources, XObject construction from image/white-rect helpers, XObject execution, and XObject name lookup are implemented for the migrated slice. Open tasks: PNG ancillary chunks outside color management, true delayed PNG file loading parity for `LoadPngImageFromFile2`, broader external-image compatibility fixtures, and reader-level visual/structural comparisons.
- Document features: document manager lifecycle helpers, page tree configuration, page boundary/rotate/zoom metadata, outlines, destinations, link/URI/text/free-text/line/square/circle/text-markup/popup/stamp/projection/widget/basic 3D annotations, annotation color/markup/callout/line-caption setters, annotation appearance form streams with named N/R/D states and local Font/XObject resources, specialized white-while-print widget appearances, page labels, named dictionaries, attachments, embedded files, JavaScript actions, viewer preferences, slide-show page transitions, PDF/A metadata/output-intent generation and save-time validation, output intents, ICC loading aliases, U3D streams, 3D views/nodes/camera/cross-sections, C3D/PD3 measure and ExData dictionaries, axial/radial/free-form triangle mesh shadings, and extended graphics state are implemented for the migrated slice. Open tasks: PDF/A action/media prohibitions beyond current save-time checks, broader name-tree mutation/re-save coverage, and remaining long-tail annotation and 3D variants not present in the local C demos.
- Security: revision 2/3 Standard Security includes validation, authentication, object encryption, and object decryption helpers. Open task: revisit only if a newer upstream source introduces later security-handler variants; the local C source exposes none.
- Compatibility demos: the canonical C PDF demo sources now have managed smoke ports with generated PDF structural checks, a generated inventory under `artifacts/compatibility-demos/libharu-demo-inventory.txt`, optional first-page Poppler render checks against checked pixel-profile fixtures when `pdftoppm` is on PATH, and a manifest for exact upstream PDF/hash comparisons as fixtures become available. Open tasks: broaden rendered fixture coverage and populate exact C output fixture rows when stable upstream outputs are checked in.
- Public API facade: the managed `HPdf` facade now has callable coverage for the exported function names in the local `include/hpdf.h` audit. Several low-level handles are represented by managed equivalents (`PdfFontDef`, `PdfContentStream`, `PdfExData`) and the latest parity pass added generated predefined CJK CMap/Unicode data, generated single-byte encoder maps, generated Base14/CID metric tables, specialized and named annotation appearance dictionaries with resources, expanded PDF/A rule smoke coverage, large name-tree fixtures, long-tail 3D helper coverage, save-time actual-use TrueType font-program subsets, optional checked visual render fixtures, and an exact reference-output fixture manifest.

## Source Module Map

| C module group | Managed destination | Status |
| --- | --- | --- |
| Core objects, dictionary, array, primitive values | `csharp/LibHaru/Internal` | Started |
| Streams, xref, trailer writer | `csharp/LibHaru/Internal`, `PdfDocument` | Started |
| Document/catalog/pages/page operators | `csharp/LibHaru/Public` | Started |
| Error, memory manager, list utilities | `HaruError`, `HaruStatus`, managed collections | Error started |
| Base14 fonts | `PdfFont`, `Base14Fonts`, `PdfFontProgram`, generated Base14 data | Exact widths/descriptors started |
| Type1 fonts | `Type1FontLoader`, `PdfFontProgram`, `PdfDocument` | Started |
| TrueType fonts | `TrueTypeFontLoader`, `PdfFontProgram`, `PdfDocument` | Dense subset/remap path started |
| CID fonts | `PdfDocument`, `PdfFontProgram`, `PredefinedCidFonts`, generated CJK metrics | Type0 TrueType CID maps and predefined CJK CID names/metrics started |
| Encoders and CMaps | `PdfEncoding`, generated single-byte maps, generated CJK CMap data, generated ToUnicode streams | Single-byte, UTF/Identity, predefined CJK CMap names/tables, and save-time ToUnicode maps started |
| Images and color spaces | `PdfDocument`, `PdfImage`, `PngImageLoader`, `CcittFaxEncoder`, `PdfPage` | Started |
| Stream filters and compression | `PdfStreamObject`, `PdfStreamFilter` | Started |
| Encryption | `PdfEncryption`, `PdfDocument`, `PdfWriter` | Started |
| PDF/A, metadata, output intents | `PdfDocument`, `PdfIccProfile`, `PdfOutputIntent` | Started |
| Outlines, destinations, annotations | `PdfDestination`, `PdfOutline`, `PdfAnnotation`, `PdfPage`, `PdfDocument` | Started |
| Names, attachments, JavaScript, PDF/A, output intents | `PdfEmbeddedFile`, `PdfJavaScript`, `PdfOutputIntent`, `PdfDocument` | Started |
| U3D, 3D measure, shadings, ext graphics state | `PdfU3D`, `Pdf3DView`, `PdfShading`, `PdfExtGState`, `PdfPage` | Started |
